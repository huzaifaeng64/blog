

<%= form_with model: @article, local: true do |form1| %>
 
  <% if @article.errors.any? %>
  <div id="error_explanation">
    <h2>
      <%= pluralize(@article.errors.count, "error") %> prohibited this article from being saved:
    </h2>
    <ul>
      <% @article.errors.full_messages.each do |msg| %>
      <li><%=msg %></li>
      <% end %>
    </ul>
  </div>
  <% end %>


  <p>
    <%= form1.label :title %><br>
    <%= form1.text_field :title %>
  </p>

  <p>
    <%= form1.label :text %><br>
    <%= form1.text_area :text %>
  </p>
  <p>
    <%= form1.fields_for :comments do |builder| %>
      <%= render 'comment', f: builder %>
    <% end %>
  </p>
<p>

  <%= form1.submit %>
</p>


<% end %>